/**
 * Artifex Labs — PDF Design System
 * ------------------------------------------------------------------
 * One import surface for the whole system: color/spacing/type tokens,
 * the typography scale (with automatic brand-font embedding + safe
 * fallback), primitives, and composed components.
 *
 *   import { color, space, type, SectionHeader, Card } from "@/lib/pdf/design";
 */
export * from "./tokens";
export * from "./typography";
export * from "./primitives";
export * from "./components";
export * from "./atmosphere";
